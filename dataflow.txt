init server -> user conn
(maybe welcome message and instruction) #if you can guess the whole word youll get bonus when that word hasn't been solve more than half
(user may reset game anytime maybe using signal like ^R)
--> enter name from [client]
--send name to server--> [client] init (display hangman state)
--> [server] init game by name
--> [server] send data from initial game (dash, guess, used letter)
--[client] send guess--v

    server check integrity of data guess
if correct:
    --> check with secret_word --> [server] update data & send back to client
    --> [client] update game state(dash,guess,used letter)
else:
    --> [server] not update state --> [server] send error message back to client


ืinit print alldata
input
check if reset game is invoke => reset()
check integrity input
    if ok:
        check_guess() 
            |
            v
        if correct & wholeword & dash < len(secret_word/2):
            get_bonus()
        else:
            ... normal score()
        end() => wanna play again? reset()
    else:
        send error msg back
        input again